# 🔒 Variables d'environnement pour la production (VPS)
# Copiez ce fichier en .env.production sur le VPS et remplissez les valeurs

# 🚨 IMPORTANT: Ne jamais committer le fichier .env.production réel !

# 🔐 Authentification NextAuth
NEXTAUTH_SECRET=votre_secret_très_long_et_généré_avec_openssl_rand_base64_32
NEXTAUTH_URL=https://votre-domaine.com

# 🗄️ Base de données PostgreSQL
# ⚠️ IMPORTANT: Utiliser "postgres" comme hostname (pas localhost) pour Docker networking
DATABASE_URL=postgresql://postgres:VOTRE_MOT_DE_PASSE_POSTGRES@postgres:5432/codecrafting?schema=public

# Variables PostgreSQL pour docker-compose.yml
POSTGRES_DB=codecrafting
POSTGRES_USER=postgres
POSTGRES_PASSWORD=VOTRE_MOT_DE_PASSE_POSTGRES

# 🔑 Google OAuth (optionnel)
GOOGLE_CLIENT_ID=votre_google_client_id
GOOGLE_CLIENT_SECRET=votre_google_client_secret
GOOGLE_ADMIN_EMAILS=admin@votre-domaine.com,votre-email@gmail.com
GOOGLE_ALLOWED_DOMAINS=votre-domaine.com,gmail.com

# 🌍 Configuration de l'environnement
NODE_ENV=production
NEXT_PUBLIC_ENV=production

# 🚀 Configuration du serveur
PORT=3000

# 🔧 Configuration pour le déploiement
# Ces variables doivent être exportées dans le shell avant d'exécuter deploy.sh
# export GITHUB_TOKEN="votre_token_github_personnel_ou_d'organisation"
# export POSTGRES_PASSWORD="VOTRE_MOT_DE_PASSE_POSTGRES"

# 📝 Instructions de déploiement:
# 1. Copiez ce fichier: cp env.production.example .env.production
# 2. Remplissez toutes les valeurs ci-dessus
# 3. Exportez les variables pour le déploiement:
#    export GITHUB_TOKEN="ghp_xxxxxxxxxxxxxxxxxxxx"
#    export POSTGRES_PASSWORD="MotDePasseSuperSecret123!"
# 4. Exécutez: ./deploy.sh

# 🔒 Génération du NEXTAUTH_SECRET:
# openssl rand -base64 32

# ⚠️ Sécurité:
# - Utilisez des mots de passe forts (minimum 20 caractères, chiffres, symboles)
# - Faites rotation des secrets régulièrement
# - Limitez les permissions du fichier .env.production (chmod 600)
# - N'utilisez jamais les valeurs de développement en production